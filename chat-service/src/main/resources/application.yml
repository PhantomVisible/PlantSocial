server:
  port: 8083

spring:
  application:
    name: chat-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8082/realms/plantsocial
  data:
    redis:
      host: localhost
      port: 6379
